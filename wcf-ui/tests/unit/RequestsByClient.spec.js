import { shallowMount } from '@vue/test-utils';
// @ts-ignore
import RequestsByClient from '@/components/RequestsByClient.vue';

describe('RequestsByClient.vue', () => {
  it('displays features for selected client', () => {
    const wrapper = shallowMount(RequestsByClient, {
    });
    // expect(wrapper)
  });
});
